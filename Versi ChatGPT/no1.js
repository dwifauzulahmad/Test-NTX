const fs = require('fs');

// Membaca data dari file
const data = fs.readFileSync('./files/dataset.json', 'utf-8');
const dataset = JSON.parse(data);

// Refaktorisasi refactoreMe1
function refactoreMe1() {
  // Lakukan operasi pada dataset
}

// Refaktorisasi refactoreMe2
function refactoreMe2() {
  // Lakukan operasi pada dataset
}
